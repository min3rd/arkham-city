import { CdkListItem } from './cdk-list-item.directive';

describe('CdkListItemDirective', () => {
  it('should create an instance', () => {
    const directive = new CdkListItem();
    expect(directive).toBeTruthy();
  });
});
